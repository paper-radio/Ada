# Uncomment the next line to define a global platform for your project
# platform :ios, '12.0'

target 'Ada' do
    # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
    use_frameworks!

    # Pods for Ada
    pod 'AudioKit'

    target 'AdaTests' do
        inherit! :search_paths
        # Pods for testing
    end

    target 'AdaUITests' do
        inherit! :search_paths
        # Pods for testing
    end
end
